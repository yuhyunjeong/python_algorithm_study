def solution(stones): #stones - [2, 5, 1, 3, 2, 1]
    cnt = 0
    current = 0
    n = len(stones)
    while current < n:

        current += stones[current] #리스트에 있는 값 만큼 건넌다
        cnt += 1

    return cnt